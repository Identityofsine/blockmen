// This file will contain a UI Class that acts as a UI component, this will allow us to create specific things and then eventually let us create a UI state function that updates the values stored within the UI to update.

import { RenderStateObject } from "../objects/renderstateobject";

export class UIService {
	//state functions
	private readonly stateFunctions: RenderStateObject[] = [];



}
