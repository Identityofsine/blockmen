{
  "type": "object",
  "required": ["type", "data"],
  "properties": {
    "type": {
      "type": "string",
      "enum": ["chat"]
    },
    "data": {
      "type": "object",
      "required": ["message", "destination"],
      "properties": {
        "message": {
          "type": "string"
        },
        "destination": {
          "type": "string",
          "enum": ["group", "all"]
        }
      }
    }
  }
}
